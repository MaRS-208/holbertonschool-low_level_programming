#include "main.h"

/**
 * _abs - absolute numbers
 * @n: number
 * Return: n
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
		if (n == 0)
		{
			return (n);
		}
		else
			return (-n);
}

